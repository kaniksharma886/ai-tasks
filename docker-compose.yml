services:
  ai-ui:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: datacom_streamlit_app
    ports:
      - "8001:8001"

    environment:
      - PYTHONUNBUFFERED=1

volumes:
  chat-data: